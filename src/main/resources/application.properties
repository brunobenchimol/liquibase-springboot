spring.main.banner-mode=off
logging.level.org.springframework=ERROR

spring.datasource.url=${LIQUIBASE_URL}
spring.datasource.username=${LIQUIBASE_USERNAME}
spring.datasource.password=${LIQUIBASE_PASSWORD}

management.endpoints.health.sensitive=false
management.health.db.enabled=true
management.health.defaults.enabled=true
management.endpoint.health.show-details=always

preliquibase.enabled=true

# Set the schema to use. We have to tell both Hibernate and Liquibase this.
spring.jpa.properties.hibernate.default_schema=${LIQUIBASE_DEFAULT_SCHEMA_NAME}
spring.liquibase.default-schema=${LIQUIBASE_DEFAULT_SCHEMA_NAME}
spring.liquibase.liquibase-schema=${LIQUIBASE_DEFAULT_SCHEMA_NAME}